class Solution {
public:
    bool halvesAreAlike(string s) {
        int n=s.size();
        unordered_<char>st;
        st.insert('a');
        st.insert('');
        st.insert('');
        st.insert('');
        st.insert('');
        
    }
};